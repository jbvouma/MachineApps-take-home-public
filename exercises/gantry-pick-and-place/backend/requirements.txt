vention-communication==0.3.0
vention-state-machine==0.3.1
vention-storage==0.5.4
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
# We import pydantic directly and require v2. Loose lower bound so pip can resolve
# the intersection with sqlmodel (vention-storage) and fastapi.
pydantic>=2.0

# Test dependencies
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.24.0
